# 怎么用我（Cursor Cloud Agent）—— 通俗易懂指南

你好！我是你的 Cursor Cloud Agent（云端编程助手）。简单理解：你给我一个任务，我会在云端的虚拟机里替你写代码、改代码、跑命令、提交 PR。你不用一直盯着，等我做完再来看结果就行。

下面这份指南手把手教你怎么和我配合。

---

## 一、我能帮你做什么

| 类型 | 例子 |
| --- | --- |
| 写新功能 | "帮我加一个登录页面，要支持邮箱+密码" |
| 改 Bug | "用户列表加载时会报 500，帮我排查并修复" |
| 重构代码 | "把 utils.py 里的函数按模块拆分到不同文件" |
| 写测试 | "给 order_service.py 写单元测试，覆盖率到 80%" |
| 写文档 | "给这个项目写一份 README，包括安装、使用、贡献指南" |
| 升级依赖 | "把 React 17 升级到 React 18，并修复破坏性变更" |
| 调研问题 | "帮我看一下这个项目的目录结构，告诉我核心模块在哪" |

只要是和代码、文档、配置相关的活，都可以扔给我。

---

## 二、怎么给我下达任务（写好 Prompt 是关键）

### 1. 一个好的任务请求 = 目标 + 范围 + 约束

**反面教材：**
> 优化一下代码

太模糊，我不知道优化什么、优化到什么程度。

**正面教材：**
> 帮我优化 `src/api/user.ts` 里的 `getUserList` 函数。
> 目前查询 1 万条数据要 5 秒，希望降到 1 秒以内。
> 约束：不要引入新的依赖，不要改数据库结构。

清晰的目标（性能优化到 1 秒）、明确的范围（具体文件、具体函数）、明确的约束（不引入依赖）。

### 2. 复杂任务可以分阶段

如果任务很大，比如"做一个完整的电商后台"，建议你先让我做调研：

> 先别动代码，帮我看一下当前项目结构，列出实现一个商品管理模块需要改哪些文件，给我一个方案。

我给你方案后，你确认了再让我动手。

### 3. 提供上下文

- 用 `@文件名` 或 `@文件夹` 来指引我看哪个文件
- 把报错信息、日志原文贴给我，别只说"报错了"
- 有相关的 issue 链接、设计文档，直接贴 URL

---

## 三、我的工作方式

1. **我跑在云端的独立虚拟机里**，不会动你本地的环境。
2. **我会自己创建分支**（比如 `cursor/xxx-yyy`），不会污染你的 `main`。
3. **我做完会自动提交 commit 并推送到远端**，然后开一个 Draft PR 给你 review。
4. **你随时可以来留言**，我会看到并继续干活。

---

## 四、典型协作流程

```
你下达任务
   ↓
我读代码、做调研、写代码
   ↓
我跑测试 / 跑 lint
   ↓
我 commit + push + 开 PR
   ↓
你 review PR
   ↓
你留言："这里再改一下 XXX"
   ↓
我继续改 → 再 push → 更新 PR
   ↓
你满意 → merge
```

---

## 五、关于环境和密钥

- 如果项目需要数据库、API Key 之类的密钥，**不要直接贴到聊天里**。
- 去 [Cursor Dashboard](https://cursor.com/dashboard) 的 **Cloud Agents > Secrets** 添加，密钥会以环境变量形式注入到我的虚拟机里，安全可靠。
- 如果环境老是缺依赖，可以让我帮你跑一次 [cursor.com/onboard](https://cursor.com/onboard) 的"环境配置 Agent"，它会帮你把基础镜像和启动脚本配好，下次别的 Agent 就不用重复装环境了。

---

## 六、和我合作的小技巧

### 多用"显式约束"

- "**不要修改** `src/legacy/` 下的任何文件"
- "**必须保持** API 向后兼容"
- "**只改** TypeScript 文件，别动 CSS"

### 让我先解释，再动手

- "在动手之前，先告诉我你的修复思路"
- "列出你打算修改的文件清单，等我确认"

### 阶段性验收

- "做完第一步先 push，等我看了再继续"

### 出错了别怕

- 我跑命令失败、测试挂了，会自己尝试修复并继续。
- 如果实在搞不定，我会停下来告诉你卡在哪。

---

## 七、什么情况我可能搞不定

- 任务描述太模糊，我会按自己的理解去做，结果可能跑偏 → **请写清楚需求**
- 缺密钥/凭证，没法跑外部服务 → **请去 Dashboard 配 Secret**
- 涉及到需要人工判断的产品决策（比如 UI 风格选哪种）→ **请你直接告诉我选哪个**
- 我不能直接和你打电话/视频，所有沟通都通过文字 PR 评论

---

## 八、一句话总结

**把我当成一个能力还不错、但需要你说清楚需求的远程同事。** 你说得越具体，我交付得越准。

祝合作愉快！有任务直接发过来就行，我随时待命。
